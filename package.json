{
  "name": "track-helmer",
  "version": "0.1.0",
  "description": "First-party tracking + Meta Conversions API (CAPI) gateway + attribution dashboard (Utmify-style, self-hosted)",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "sync-spend": "node src/sync-spend.js"
  },
  "dependencies": {
    "fastify": "^5.1.0",
    "@fastify/cors": "^10.0.1",
    "@fastify/static": "^8.0.3",
    "pg": "^8.13.1"
  }
}
